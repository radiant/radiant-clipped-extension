- include_stylesheet 'admin/assets' 

%h1= t("assets_extension.remove_asset")
%p= t("assets_extension.confirm_remove_asset")

%table.index#assets{:cellpadding => "0", :cellspacing => "0", :border => "0"}
  %tbody
    %tr[@asset]
      %td.icon= image_tag @asset.thumbnail(:icon), :title => @asset.asset_file_name
      %td.name= @asset.title

- form_for [:admin, @asset], :html => { :method => 'delete' } do
  .buttons
    %input.button{:type => "submit", :value => t("assets_extension.delete_asset")}
    = t("or")
    = link_to t("cancel"), admin_assets_path